package authoring;

import javafx.scene.layout.Pane;

//popup window which allows one to modify different parameters in the level which they are editing
public class LevelModifier extends Pane {
	private int myLevel;
	
	
	public LevelModifier(int level) {
		myLevel = level;
		
	}
}
